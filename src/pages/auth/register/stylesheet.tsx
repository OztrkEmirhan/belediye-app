import {
    StyleSheet
} from "react-native";

const stylesheet = StyleSheet.create({
    goBack: {
        justifyContent: "center",
        alignItems: "center",
        borderRadius: 21,
        height: 42,
        width: 42
    },
    headerTitle: {
        position: "absolute"
    }
});
export default stylesheet;
