import {
    StyleSheet
} from "react-native";

export const stylesheet = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center',
        borderRadius: 12,
        marginBottom: 10,
        paddingRight:20,
        borderWidth: 1,
        width: '100%',
    },
    input: {
        paddingHorizontal: 15,
        paddingVertical: 14,
        fontSize: 16,
        padding: 0,
        margin: 0,
        flex: 1,
        gap:8,
    },
});