import {
    StyleSheet
} from "react-native";

export const stylesheet = StyleSheet.create({
    container: {
        borderRadius: 10,
        marginBottom: 10,
        borderWidth: 1,
        width: '100%',
    },
    input: {
        paddingHorizontal: 15,
        paddingVertical: 13,
        fontSize: 16,
        padding:0,
        margin:0,
    },
});